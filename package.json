{
  "name": "buanasphere",
  "version": "1.0.0",
  "private": true,
  "description": "Buanasphere — 3D Geospatial Intelligence & Multi-App Planetary Platform on Cloudflare Workers & Svelte 5",
  "type": "module",
  "packageManager": "bun@1.4.0",
  "engines": {
    "bun": ">=1.4.0",
    "node": "DO_NOT_USE_NODE"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "preinstall": "bun ./scripts/ensure-bun.ts",
    "check:runtime": "bun ./scripts/ensure-bun.ts",
    "dev": "bun ./scripts/ensure-bun.ts && bun x concurrently -k -n \"BE,FE\" -c \"blue.bold,green.bold\" \"bun run dev:be\" \"bun run dev:fe\"",
    "dev:be": "cd backend && bun run dev",
    "dev:fe": "cd frontend && bun run dev",
    "test": "bun test backend && bun test frontend",
    "build": "(cd backend && bun run build 2>/dev/null || true) && (cd frontend && bun run build)",
    "check": "(cd backend && (bun run check 2>/dev/null || tsc --noEmit)) && (cd frontend && bun run check)"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
